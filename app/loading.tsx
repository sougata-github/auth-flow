import { Loader } from "lucide-react";

const loading = () => {
  return (
    <div>
      <Loader className="animte-spin size-6 text-black/60 transition-all" />
    </div>
  );
};

export default loading;
